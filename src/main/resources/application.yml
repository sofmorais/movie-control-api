spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/moviecontrol
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none

  flyway:
    enabled: true
    url: jdbc:postgresql://localhost:5432/moviecontrol  # Mesma URL do datasource
    username: postgres
    password: postgres
    locations: classpath:db/migration
